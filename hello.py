#!/usr/bin/env python3
"""Print 'Hello World!' and exit."""

from posix import *
from io import *
from itertools import *
from socket import *

dict.update(globals(),zip(map(chr,count(97)),pipe()+socketpair()))
fork()and d.send(b'Hello World!\n')or fork()and print(next(open(++a,
'r')))or BufferedWriter.write(open(--b,'wb'),c.recv(1<<10))
